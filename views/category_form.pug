extends layout

block content
    h1=title
    form(method='POST')
        div.form-group
            label(for="name") Name:
            input.form-control(type='text', placeholder='Ie. Sedan', name="name", required value=(undefined===category ? '':category.name))
            label(for="commercial") Commercial:
            input.checkbox-input(type="checkbox", name='commercial')
        div.form-group
            button.btn.btn-primary(type='submit') Submit


    if errors
        p FORM NOT SUBMITTED! Please correct the following:
        ul 
            for error in errors 
                li!= error.msg